using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Constants {
    public enum ArmorType {
        Unarmored = 0,
        Light,
        Medium,
        Heavy
    }
}